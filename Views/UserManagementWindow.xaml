<Window x:Class="食品信息管理系统.Views.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="用户与角色权限管理" Height="700" Width="1100"
        MinHeight="560" MinWidth="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <TabControl>
            <TabItem Header="用户管理">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button x:Name="BtnRefreshUsers" Content="刷新" Tag="refresh_btn" Width="90" Margin="0,0,8,0" Click="RefreshUsers_Click"/>
                        <Button x:Name="BtnAddUser" Content="新增" Tag="add_btn" Width="90" Margin="0,0,8,0" Click="AddUser_Click"/>
                        <Button x:Name="BtnEditUser" Content="编辑" Tag="edit_btn" Width="90" Margin="0,0,8,0" Click="EditUser_Click"/>
                        <Button x:Name="BtnDeleteUser" Content="删除" Tag="delete_btn" Width="90" Margin="0,0,8,0" Click="DeleteUser_Click"/>
                    </StackPanel>
                    <DataGrid x:Name="UsersGrid"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              SelectionUnit="FullRow">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                            <DataGridTextColumn Header="用户名" Binding="{Binding Username}" Width="140"/>
                            <DataGridTextColumn Header="昵称" Binding="{Binding Nickname}" Width="120"/>
                            <DataGridTextColumn Header="部门" Binding="{Binding Department}" Width="120"/>
                            <DataGridTextColumn Header="角色ID" Binding="{Binding RoleId}" Width="90"/>
                            <DataGridTextColumn Header="角色名" Binding="{Binding RoleName}" Width="120"/>
                            <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="90"/>
                            <DataGridTextColumn Header="最后登录" Binding="{Binding LastLogin}" Width="170"/>
                            <DataGridTextColumn Header="本月登录" Binding="{Binding LoginCountMonth}" Width="90"/>
                            <DataGridTextColumn Header="累计登录" Binding="{Binding LoginCountTotal}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </TabItem>

            <TabItem Header="角色与权限">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <DockPanel Grid.Column="0">
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                            <Button x:Name="BtnRefreshRoles" Content="刷新角色" Tag="refresh_btn" Width="90" Margin="0,0,8,0" Click="RefreshRoles_Click"/>
                            <Button x:Name="BtnAddRole" Content="新增角色" Tag="add_btn" Width="90" Margin="0,0,8,0" Click="AddRole_Click"/>
                            <Button x:Name="BtnEditRole" Content="编辑角色" Tag="edit_btn" Width="90" Margin="0,0,8,0" Click="EditRole_Click"/>
                            <Button x:Name="BtnDeleteRole" Content="删除角色" Tag="delete_btn" Width="90" Margin="0,0,8,0" Click="DeleteRole_Click"/>
                        </StackPanel>
                        <DataGrid x:Name="RolesGrid"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow"
                                  SelectionChanged="RolesGrid_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                                <DataGridTextColumn Header="角色名" Binding="{Binding Name}" Width="130"/>
                                <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>

                    <DockPanel Grid.Column="2">
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                            <Button x:Name="BtnSyncPermissions" Content="同步权限" Tag="refresh_btn" Width="90" Margin="0,0,8,0" Click="SyncPermissions_Click"/>
                            <Button x:Name="BtnSelectAllPermissions" Content="全选" Width="90" Margin="0,0,8,0" Click="SelectAllPermissions_Click"/>
                            <Button x:Name="BtnUnselectAllPermissions" Content="全不选" Width="90" Margin="0,0,8,0" Click="UnselectAllPermissions_Click"/>
                            <Button x:Name="BtnSaveRolePermissions" Content="保存分配" Tag="save_btn" Width="90" Margin="0,0,8,0" Click="SaveRolePermissions_Click"/>
                        </StackPanel>
                        <DataGrid x:Name="PermissionsGrid"
                                  AutoGenerateColumns="False"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False">
                            <DataGrid.Columns>
                                <DataGridCheckBoxColumn Header="选中" Binding="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70" IsReadOnly="True"/>
                                <DataGridTextColumn Header="权限键" Binding="{Binding PermissionKey}" Width="220" IsReadOnly="True"/>
                                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="140" IsReadOnly="True"/>
                                <DataGridTextColumn Header="类型" Binding="{Binding NodeType}" Width="80" IsReadOnly="True"/>
                                <DataGridTextColumn Header="节点" Binding="{Binding NodeCode}" Width="120" IsReadOnly="True"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
